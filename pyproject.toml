[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "MVP_Lazy_PA"
version = "0.0.0"
description = "Product analytic"
readme = "README.md"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["app"]